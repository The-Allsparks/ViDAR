[project]
name = "vidar"
version = "0.1.0"
description = "FTC vision library — off-robot Python experiments"
readme = "README.md"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["tests/pure"]
